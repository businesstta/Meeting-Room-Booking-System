APP_PORT=5173
POSTGRES_DB=meeting_room_booking
POSTGRES_USER=meeting_app
POSTGRES_PASSWORD=replace-with-a-long-random-password
SESSION_SECRET=replace-with-a-long-random-secret
SESSION_TIMEOUT_MINUTES=30
BOOTSTRAP_ADMIN_NAME=System Administrator
BOOTSTRAP_ADMIN_USERNAME=admin
BOOTSTRAP_ADMIN_EMAIL=admin@company.test
BOOTSTRAP_ADMIN_PASSWORD=replace-with-a-unique-password-of-at-least-12-characters

# Production is HTTPS-only so session cookies must remain secure.
COOKIE_SECURE=true
CORS_ALLOWED_ORIGINS=https://office.atoz.com.mm,https://localhost

EMAIL_ENABLED=false
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
MAIL_FROM=
